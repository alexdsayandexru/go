syntax = "proto3";

package user_v1;

option go_package = "user_v1";

message Request {
    string text = 1;
    string subtext = 2;
}

message Response {
    string text = 1;
    string subtext = 2;
}

service MessageService {
    rpc SayIt (Request) returns (Response);
}